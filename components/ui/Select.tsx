import * as React from 'react'; export function Select({defaultValue,onValueChange,children}:{defaultValue?:string,onValueChange?:(v:string)=>void,children:React.ReactNode}){const items=React.Children.toArray(children).filter(Boolean) as React.ReactElement[]; const [val,setVal]=React.useState<string>(defaultValue || (items[0]?.props?.value ?? '')); React.useEffect(()=>{onValueChange&&onValueChange(val)},[val]); return <select className='h-9 w-full rounded-xl border px-3 text-sm' value={val} onChange={(e)=>setVal(e.target.value)}>{items.map((it,idx)=>(<option key={idx} value={it.props.value}>{it.props.children}</option>))}</select>} export function SelectItem({children}:{value:string,children:React.ReactNode}){return <>{children}</>}