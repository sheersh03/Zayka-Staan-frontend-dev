import * as React from 'react'; const Ctx=React.createContext<{value:string,set:(v:string)=>void}|null>(null); export function Tabs({value,onValueChange,children}:{value:string,onValueChange:(v:string)=>void,children:React.ReactNode}){return <Ctx.Provider value={{value,set:onValueChange}}><div>{children}</div></Ctx.Provider>} export function TabsList({children,className=''}:{children:React.ReactNode,className?:string}){return <div className={`rounded-xl border p-1 bg-slate-50 grid ${className}`}>{children}</div>} export function TabsTrigger({value,children,className=''}:{value:string,children:React.ReactNode,className?:string}){const ctx=React.useContext(Ctx)!;const active=ctx.value===value;return <button onClick={()=>ctx.set(value)} className={`rounded-lg px-3 py-2 text-sm ${active?'bg-white border shadow':'text-slate-600'} ${className}`}>{children}</button>} export function TabsContent({value,children,className=''}:{value:string,children:React.ReactNode,className?:string}){const ctx=React.useContext(Ctx)!;if(ctx.value!==value) return null;return <div className={className}>{children}</div>}